/* Was ist eine Datenbank

Abfragen mit SELECT * FROM Datenbanktabelle

Rechnungen mit +, -, *, /

PI() ist vorhanden

AND & OR Operatoren

String funktionen:

replace, repeat, reverse, Substring(name, startzeichen (index = 1), endzeichen)

length = anzahl zeichen

upper & lower fuer zeichen uppercase/ lowercase



SELECT * FROM Tabelle WHERE (where = diese kriterien muessen zutreffen -> kriterienabfrage)

AS zur alias vergabe zB. SELECT Name AS Hauptstadt FROM country

GROUP BY -> Gruppieren von Eintraegen mit kriterium

ORDER BY -> Sortierung

LIMIT -> Anzeige bis zum eingegebenen Wert


FRAGEN:

Wer hat die Datenbanken-SQL Sprache entwickelt?  ---- Antwort: IBM

Was war die erste Datenbank?

Was ist eine Subabfrage? Wie sieht eine Subabfrage aus?


